# PulseRAG v5 — Streamlit Cloud compatible requirements
# torch is a transitive dep of sentence-transformers.
# Pin CPU-only build to avoid 2GB+ CUDA download that causes OOM on Streamlit Cloud.
--extra-index-url https://download.pytorch.org/whl/cpu

groq>=0.9.0
chromadb>=0.5.0
sentence-transformers>=2.7.0
torch>=2.0.0
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.26.0
streamlit>=1.35.0
evidently>=0.4.30
xgboost>=2.0.0
pdfplumber>=0.10.0
python-dotenv>=1.0.0
mlflow>=2.11.0
pytest>=8.0.0